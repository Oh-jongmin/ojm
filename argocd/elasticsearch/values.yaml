volumePermissions:
  enabled: true

persistence:
  enabled: true
  storageClass: gp2-csi
  size: 2Gi

coordinating:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi

data:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi

ingest:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi

master:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi

